{
  "$defs": {
    "AgentEventKind": {
      "description": "Payload of a [`RuntimeEvent::Agent`] — one step of the agent loop. Defined as\ntransport-neutral data; the host maps its own agent events onto these.",
      "oneOf": [
        {
          "properties": {
            "kind": {
              "const": "turn_started",
              "type": "string"
            },
            "turn": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "turn"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "const": "message",
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "text"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "const": "reasoning",
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "text"
          ],
          "type": "object"
        },
        {
          "properties": {
            "arguments": true,
            "kind": {
              "const": "tool_started",
              "type": "string"
            },
            "tool": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "tool",
            "arguments"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "const": "tool_finished",
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "output": {
              "type": "string"
            },
            "tool": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "tool",
            "ok",
            "output"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "const": "interrupted",
              "type": "string"
            },
            "reason": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "reason"
          ],
          "type": "object"
        },
        {
          "properties": {
            "cache_creation_tokens": {
              "description": "Prompt tokens written into the provider's prompt cache, when reported.",
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "cache_read_tokens": {
              "description": "Prompt tokens served from the provider's prompt cache, when reported.\nAdditive and optional: producers that don't track caching omit it.",
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "input_tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "kind": {
              "const": "usage",
              "type": "string"
            },
            "output_tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "input_tokens",
            "output_tokens"
          ],
          "type": "object"
        }
      ]
    },
    "ApprovalMode": {
      "description": "Per-session approval posture controlling how high a [`RiskTier`] the gate may\nauto-approve without prompting. Pure protocol data; the host gate (P2) maps\neach tool's tier against [`Self::max_auto_tier`].",
      "oneOf": [
        {
          "const": "always_ask",
          "description": "Prompt for everything above read-only.",
          "type": "string"
        },
        {
          "const": "write",
          "description": "Auto-approve reads and edits; prompt for exec.",
          "type": "string"
        },
        {
          "const": "yolo",
          "description": "Auto-approve everything, including exec.",
          "type": "string"
        }
      ]
    },
    "AuthEventKind": {
      "description": "Authentication lifecycle event kind. Defined as pure protocol data; hosts map\nconcrete credential/login implementation details onto these states.",
      "enum": [
        "login_pending",
        "login_completed",
        "login_failed",
        "credential_refreshed"
      ],
      "type": "string"
    },
    "AuthStartFlow": {
      "description": "Login flow requested by `auth.start`.",
      "oneOf": [
        {
          "const": "browser",
          "description": "Browser authorization-code + PKCE flow over loopback/manual paste.",
          "type": "string"
        },
        {
          "const": "device_code",
          "description": "OAuth 2.0 device authorization flow for remote/mobile clients.",
          "type": "string"
        }
      ]
    },
    "BudgetSpec": {
      "description": "The fleet budget envelope (design §6). C1 threads it as a placeholder; C3\nwires the deterministic debit/cancel loop. All fields optional — an omitted\nbudget caps nothing (the engine still bounds concurrency separately).",
      "properties": {
        "max_total_cost_usd": {
          "description": "Total USD ceiling across the whole tree.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "max_total_tokens": {
          "description": "Total token ceiling across the whole tree.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_workers": {
          "description": "Maximum number of workers across the whole tree (the global semaphore).",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ContextSplit": {
      "description": "How a [`Strategy::MapReduce`] splits its shared context across map workers.\nDefined-ahead for C5; C1 never reads it.",
      "oneOf": [
        {
          "description": "Split a build_context selection into `n` roughly equal shards.",
          "properties": {
            "kind": {
              "const": "shards",
              "type": "string"
            },
            "n": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "n"
          ],
          "type": "object"
        },
        {
          "description": "One map worker per listed path group.",
          "properties": {
            "groups": {
              "items": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "type": "array"
            },
            "kind": {
              "const": "paths",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "groups"
          ],
          "type": "object"
        }
      ]
    },
    "DelegateAutonomy": {
      "description": "Autonomy posture handed to a delegated external agent CLI, mapping to each\nvendor's sandbox/permission flag: codex `--sandbox`, claude `--permission-mode`,\ngemini `--approval-mode` (read-only | edit | full). Defaults to the most\nrestricted ([`Self::ReadOnly`]) so an omitted field never grants more than read\naccess.",
      "oneOf": [
        {
          "const": "read_only",
          "description": "The delegated agent may only read; no edits, no command execution.",
          "type": "string"
        },
        {
          "const": "edit",
          "description": "The delegated agent may read and edit workspace files.",
          "type": "string"
        },
        {
          "const": "full",
          "description": "The delegated agent may read, edit, and run commands.",
          "type": "string"
        }
      ]
    },
    "DelegateRole": {
      "description": "The *role* a delegated agent plays (DA-7): a curated behavior preset the host\nmaterializes on top of the raw agent CLI. Defaults to [`Self::Standard`] (no\npreset — the `task`/`autonomy` are used verbatim).\n\n[`Self::Scout`] is a read-only **repository-exploration** preset: the host\nwraps the task in an explore-and-cite instruction and forces read-only\nautonomy, so the agent returns compact `path:line-range` citations instead of\nediting — a cheap context sub-agent that keeps the caller's context window\nclean (the FastContext pattern, run on an existing CLI). The role is plain\nvocabulary here; the host (`delegate_roles`) owns the prompt + posture it maps to.",
      "oneOf": [
        {
          "const": "standard",
          "description": "No preset: the task and autonomy are passed through unchanged.",
          "type": "string"
        },
        {
          "const": "scout",
          "description": "Read-only repository-exploration preset — forces read-only autonomy and an\nexplore-and-cite instruction (see the type docs).",
          "type": "string"
        }
      ]
    },
    "Event": {
      "description": "One node in a run's append-only tape: a logical sequence number plus the typed\nstep. `seq` is a monotonic logical clock (0,1,2,…) assigned at capture, *not* a\nwall-clock — so a replay reproduces byte-identical ordering and hashes. The\n`kind` is a nested object (`{\"seq\":N,\"kind\":{\"kind\":\"...\",...}}`) rather than\nflattened: a stable, schemars-clean `$ref` to [`EventKind`] that keeps the\nexported protocol schema deterministic.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/EventKind"
        },
        "seq": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "seq",
        "kind"
      ],
      "type": "object"
    },
    "EventKind": {
      "description": "One typed, replayable step in a captured run's tape (`trust-substrate.md`\n§5 `Event.kind`). Internally tagged (`{\"kind\": \"...\", ...}`), mirroring\n[`crate::FlowDecisionKind`] / [`crate::AgentEventKind`] so the audit trail is\ngolden-diffable. The set is intentionally small and **execution-grounded** —\nonly events a real delegated CLI run actually produces today. Tool-lifecycle\nkinds (`tool_started` / `tool_finished`) are deliberately absent: the external\nCLIs emit raw text, not parsed tool structure, so a reserved-but-unfilled kind\nwould be dead protocol. Additive: new kinds may be appended.",
      "oneOf": [
        {
          "description": "The run began: which agent, the delegated task text, and the working dir.",
          "properties": {
            "agent": {
              "type": "string"
            },
            "cwd": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "run_started",
              "type": "string"
            },
            "task": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "agent",
            "task"
          ],
          "type": "object"
        },
        {
          "description": "A turn of the delegated agent began. `turn` is a 0-based logical index.",
          "properties": {
            "kind": {
              "const": "turn_started",
              "type": "string"
            },
            "turn": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "turn"
          ],
          "type": "object"
        },
        {
          "description": "A raw stdout/stderr line from the delegated CLI — the tape unit. `turn`\nattributes it to the turn that produced it.",
          "properties": {
            "kind": {
              "const": "output",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "turn": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "turn",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "Per-turn token/cost rollup, emitted only when the agent reported usage.\nAll counts are `u64`; cost is integer micro-USD (never a float) so the\nhashed canonical bytes are stable.",
          "properties": {
            "cache_creation_tokens": {
              "default": 0,
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "cache_read_tokens": {
              "default": 0,
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "cost_micro_usd": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "input_tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "kind": {
              "const": "usage_updated",
              "type": "string"
            },
            "output_tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "turn": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "turn",
            "input_tokens",
            "output_tokens"
          ],
          "type": "object"
        },
        {
          "description": "A turn finished. `ok` is the turn's success as the host observed it.",
          "properties": {
            "kind": {
              "const": "turn_finished",
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "turn": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "turn",
            "ok"
          ],
          "type": "object"
        },
        {
          "description": "The run finished: overall success, the process exit code when known, and\nwhether it was killed by the wall-clock timeout.",
          "properties": {
            "exit_code": {
              "format": "int32",
              "type": [
                "integer",
                "null"
              ]
            },
            "kind": {
              "const": "run_finished",
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "timed_out": {
              "default": false,
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "ok"
          ],
          "type": "object"
        }
      ]
    },
    "FailPolicy": {
      "description": "What the engine does when a step's worker fails (`ok == false` or errored).",
      "oneOf": [
        {
          "description": "Stop the whole flow on this failure (the safe default).",
          "properties": {
            "kind": {
              "const": "abort",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Record the failure and continue (used by `Parallel` branches).",
          "properties": {
            "kind": {
              "const": "continue",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Retry the step up to `n` more times before applying `Abort`.",
          "properties": {
            "kind": {
              "const": "retry",
              "type": "string"
            },
            "n": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "n"
          ],
          "type": "object"
        }
      ]
    },
    "FlowDecisionKind": {
      "description": "The typed kinds a [`RuntimeEvent::FlowDecision`] can record (design §6/§8).\nA closed, additive-versioned enum so the audit trail is golden-diffable and\nreplayable; later waves add vote/judge/debate kinds.",
      "oneOf": [
        {
          "description": "The fleet budget (USD or tokens) was exhausted; the engine cooperatively\ncancelled every branch (design §6, the budget brake / fork-bomb cure).",
          "properties": {
            "kind": {
              "const": "budget_exhausted",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "A spawn was refused at the depth ceiling — the engine did not start more\nworkers because `depth >= max_depth` (design §8, absence-at-floor). A\ndeterministic, recorded refusal, not a crash.",
          "properties": {
            "depth": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "kind": {
              "const": "depth_ceiling",
              "type": "string"
            },
            "max_depth": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "depth",
            "max_depth"
          ],
          "type": "object"
        },
        {
          "description": "A spawn was refused at the worker ceiling — `live_workers >= max_workers`\nacross the whole tree (design §8, the process-global semaphore bound).",
          "properties": {
            "kind": {
              "const": "worker_ceiling",
              "type": "string"
            },
            "live_workers": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "max_workers": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "live_workers",
            "max_workers"
          ],
          "type": "object"
        },
        {
          "description": "A `VoteJudge` strategy tallied its candidates before adjudication (design §3):\nhow many candidate workers succeeded out of how many ran, and whether the\nquorum `k` was reached. The audit trail of WHAT the judge was handed. Additive\nwithin v4.",
          "properties": {
            "k": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "kind": {
              "const": "vote_tally",
              "type": "string"
            },
            "ok": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "reached": {
              "type": "boolean"
            },
            "total": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "ok",
            "total",
            "k",
            "reached"
          ],
          "type": "object"
        },
        {
          "description": "A judge (a `VoteJudge` or `Debate` strategy's adjudicator) picked an outcome\n(design §3): the judge node's id and whether it succeeded. The audit trail of\nthe adjudication itself. Additive within v4.",
          "properties": {
            "kind": {
              "const": "judge_pick",
              "type": "string"
            },
            "node_id": {
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "node_id",
            "ok"
          ],
          "type": "object"
        },
        {
          "description": "One round of a `Debate` strategy completed (design §3): the round index\n(0-based) and how many of the sides argued successfully that round. Emitted\nonce per round so the debate's progression is a replayable audit trail.\nAdditive within v4.",
          "properties": {
            "kind": {
              "const": "debate_round",
              "type": "string"
            },
            "round": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "sides_ok": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "round",
            "sides_ok"
          ],
          "type": "object"
        }
      ]
    },
    "FlowNodeUsage": {
      "description": "A flow node's token usage, carried on [`RuntimeEvent::FlowNodeFinished`].\nMirrors the token fields of [`AgentEventKind::Usage`]; cache counts are\noptional and omitted when the worker did not report caching.",
      "properties": {
        "cache_creation_tokens": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "cache_read_tokens": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "input_tokens": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "output_tokens": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "input_tokens",
        "output_tokens"
      ],
      "type": "object"
    },
    "FlowRunOutcome": {
      "description": "The aggregated outcome of a finished flow, carried on\n[`RuntimeEvent::FlowCompleted`]: whether the flow succeeded under its\njoin/fail policy, a one-line summary, and the flow's final text (the kept\nresults concatenated, in declared order).",
      "properties": {
        "final_text": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "ok",
        "summary"
      ],
      "type": "object"
    },
    "FlowWorkerKind": {
      "description": "Which worker family ran a flow node — the only place the CLI-vs-provider\ndistinction is visible to a flow client (design §2/§7). Protocol data; the\nhost maps its own worker kind onto these.",
      "oneOf": [
        {
          "const": "cli",
          "description": "An external agentic CLI (codex / claude / gemini) subprocess.",
          "type": "string"
        },
        {
          "const": "provider",
          "description": "An in-process provider loop over the Nerve tool surface.",
          "type": "string"
        },
        {
          "const": "named",
          "description": "A worker resolved from a named `WorkerDef` data file (C6, worker-as-data):\nthe concrete family is decided by the loaded def, so the declarative ref only\nknows it is `named` (the node-agent stream carries the resolved behavior).",
          "type": "string"
        }
      ]
    },
    "HostCapabilities": {
      "description": "Host-shell capabilities available to GUI/runtime clients through protocol\ncommands. These are intentionally concrete native affordances, not product\nwishes: clients should only render native integration affordances when the\nhost reports support here.",
      "properties": {
        "clipboardWriteText": {
          "type": "boolean"
        },
        "externalUrlOpen": {
          "default": false,
          "type": "boolean"
        },
        "globalHotkey": {
          "type": "boolean"
        },
        "host": {
          "type": "string"
        },
        "nativeContextMenu": {
          "type": "boolean"
        },
        "nativeDragDrop": {
          "type": "boolean"
        },
        "nativeFileDialogs": {
          "type": "boolean"
        },
        "nativeSettingsWindow": {
          "type": "boolean"
        },
        "nativeWindowChrome": {
          "type": "boolean"
        },
        "osNotifications": {
          "type": "boolean"
        },
        "platform": {
          "type": "string"
        },
        "richClipboard": {
          "type": "boolean"
        },
        "systemAccentColor": {
          "type": [
            "string",
            "null"
          ]
        },
        "systemAccentInkColor": {
          "type": [
            "string",
            "null"
          ]
        },
        "systemColorScheme": {
          "type": [
            "string",
            "null"
          ]
        },
        "workspaceReveal": {
          "type": "boolean"
        }
      },
      "required": [
        "host",
        "platform",
        "workspaceReveal",
        "nativeWindowChrome",
        "nativeSettingsWindow",
        "nativeFileDialogs",
        "globalHotkey",
        "nativeDragDrop",
        "osNotifications",
        "clipboardWriteText",
        "richClipboard",
        "nativeContextMenu"
      ],
      "type": "object"
    },
    "Join": {
      "description": "How parallel/vote results are folded — always in **declared `Step` order**,\nnever completion order (design §3, the determinism invariant).",
      "oneOf": [
        {
          "description": "Keep every branch result (in declared order).",
          "properties": {
            "kind": {
              "const": "all",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Keep the first OK result in declared order (a branch with `ok == true`).",
          "properties": {
            "kind": {
              "const": "first_ok",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Keep results once `n` branches succeed (the first `n` OKs in declared\norder); a short quorum yields whatever OKs there were.",
          "properties": {
            "kind": {
              "const": "quorum",
              "type": "string"
            },
            "n": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "n"
          ],
          "type": "object"
        }
      ]
    },
    "LedgerEntry": {
      "description": "One entry on the content-addressed spine: this event's own digest plus the\nchained digest committing to it and every prior event\n(`chained[n] = sha256(chained[n-1] || event_hash[n])`). A verifier re-derives\nthe spine from the [`Run::events`] to confirm the tape is untampered.",
      "properties": {
        "chained_hash": {
          "type": "string"
        },
        "event_hash": {
          "type": "string"
        },
        "seq": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "seq",
        "event_hash",
        "chained_hash"
      ],
      "type": "object"
    },
    "RiskTier": {
      "description": "Coarse risk classification for a tool, ordered least-to-most privileged.\nAdvisory protocol data consumed by a future permission engine (P4); it does\nnot gate execution today.",
      "oneOf": [
        {
          "const": "read_only",
          "description": "Pure reads: catalog/search/navigation that never mutate state.",
          "type": "string"
        },
        {
          "const": "edit",
          "description": "Mutates workspace files (writes, patches, moves, deletes).",
          "type": "string"
        },
        {
          "const": "exec",
          "description": "Runs arbitrary commands or otherwise escapes the file sandbox.",
          "type": "string"
        }
      ]
    },
    "Run": {
      "description": "A captured, replayable agent run — the L0 unit of trust. The ordered\n[`Self::events`] tape is the record; [`Self::ledger`] is its content-addressed\nspine; [`Self::root_hash`] (the spine head, `\"\"` for an empty tape) is the\nsingle content address. `started_at_ms` / `finished_at_ms` are host wall-clock\nmetadata for display and are **excluded from the hashed bytes** (only `events`\nare hashed), so they never perturb the content address.",
      "properties": {
        "agent": {
          "type": "string"
        },
        "events": {
          "items": {
            "$ref": "#/$defs/Event"
          },
          "type": "array"
        },
        "finished": {
          "default": false,
          "type": "boolean"
        },
        "finished_at_ms": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "ledger": {
          "default": [],
          "items": {
            "$ref": "#/$defs/LedgerEntry"
          },
          "type": "array"
        },
        "root": {
          "type": [
            "string",
            "null"
          ]
        },
        "root_hash": {
          "default": "",
          "type": "string"
        },
        "run_id": {
          "type": "string"
        },
        "schema_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "session_id": {
          "type": "string"
        },
        "started_at_ms": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "schema_version",
        "run_id",
        "session_id",
        "agent",
        "started_at_ms",
        "events"
      ],
      "type": "object"
    },
    "RuntimeCapabilities": {
      "properties": {
        "events": {
          "$ref": "#/$defs/RuntimeEventCapabilities"
        },
        "jobs": {
          "$ref": "#/$defs/RuntimeJobCapabilities"
        },
        "transport": {
          "$ref": "#/$defs/RuntimeTransportCapabilities"
        }
      },
      "required": [
        "transport",
        "events",
        "jobs"
      ],
      "type": "object"
    },
    "RuntimeCommand": {
      "description": "Transport-neutral command understood by human-facing runtime adapters.",
      "oneOf": [
        {
          "description": "Lightweight health check used by clients before opening a real session.",
          "properties": {
            "kind": {
              "const": "ping",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Return all runtime tool specifications.",
          "properties": {
            "kind": {
              "const": "tool.list",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Execute one MCP-style tool through the runtime dispatcher.",
          "properties": {
            "arguments": {
              "additionalProperties": true,
              "default": {},
              "type": "object"
            },
            "kind": {
              "const": "tool.call",
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "Run the built-in agent loop as a job. This is protocol vocabulary only:\nthe host job manager (the composition root) executes it; the core runtime\ndispatcher does not (it has no LLM/provider knowledge). Provider/model are\nplain data here and translated to domain types by the host.",
          "properties": {
            "kind": {
              "const": "agent.run",
              "type": "string"
            },
            "max_turns": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "model": {
              "type": "string"
            },
            "provider": {
              "type": "string"
            },
            "reasoning_effort": {
              "type": [
                "string",
                "null"
              ]
            },
            "system_prompt": {
              "type": [
                "string",
                "null"
              ]
            },
            "task": {
              "type": "string"
            },
            "temperature": {
              "format": "float",
              "type": [
                "number",
                "null"
              ]
            },
            "tool_filter": {
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "provider",
            "model",
            "task"
          ],
          "type": "object"
        },
        {
          "description": "Start or resume a host-managed interactive agent session.",
          "properties": {
            "agent": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "session.start",
              "type": "string"
            },
            "max_turns": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "model": {
              "type": "string"
            },
            "provider": {
              "type": "string"
            },
            "reasoning_effort": {
              "type": [
                "string",
                "null"
              ]
            },
            "resume": {
              "type": [
                "string",
                "null"
              ]
            },
            "system_prompt": {
              "type": [
                "string",
                "null"
              ]
            },
            "temperature": {
              "format": "float",
              "type": [
                "number",
                "null"
              ]
            },
            "tool_filter": {
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "workspace": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "provider",
            "model"
          ],
          "type": "object"
        },
        {
          "description": "Send a user message to an existing host-managed session.",
          "properties": {
            "kind": {
              "const": "session.message",
              "type": "string"
            },
            "session_id": {
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "Interrupt the current turn of an existing host-managed session.",
          "properties": {
            "kind": {
              "const": "session.interrupt",
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "Reply to a session approval request.",
          "properties": {
            "decision": {
              "$ref": "#/$defs/SessionApprovalDecision"
            },
            "kind": {
              "const": "session.respond",
              "type": "string"
            },
            "request_id": {
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id",
            "request_id",
            "decision"
          ],
          "type": "object"
        },
        {
          "description": "Fetch one host-managed session.",
          "properties": {
            "kind": {
              "const": "session.get",
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "List host-managed sessions.",
          "properties": {
            "kind": {
              "const": "session.list",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Close a host-managed session.",
          "properties": {
            "kind": {
              "const": "session.close",
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "Switch the model (and optionally provider) of a live session in place,\nkeeping its history and checkpoint. Takes effect from the next turn.",
          "properties": {
            "kind": {
              "const": "session.set_model",
              "type": "string"
            },
            "model": {
              "type": "string"
            },
            "provider": {
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id",
            "model"
          ],
          "type": "object"
        },
        {
          "description": "Switch the approval mode of a live session in place. Takes effect from the\nnext gate decision. Pure protocol vocabulary; the host session manager\nstores it (P2 consults it in the gate).",
          "properties": {
            "kind": {
              "const": "session.set_mode",
              "type": "string"
            },
            "mode": {
              "$ref": "#/$defs/ApprovalMode"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id",
            "mode"
          ],
          "type": "object"
        },
        {
          "description": "Start a host-managed OAuth login.",
          "properties": {
            "flow": {
              "$ref": "#/$defs/AuthStartFlow",
              "description": "Requested login flow. Defaults to browser authorization-code flow."
            },
            "kind": {
              "const": "auth.start",
              "type": "string"
            },
            "provider": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "provider"
          ],
          "type": "object"
        },
        {
          "description": "Complete a host-managed OAuth login with a code or pasted callback URL.",
          "properties": {
            "callback_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "code": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "auth.complete",
              "type": "string"
            },
            "login_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "login_id"
          ],
          "type": "object"
        },
        {
          "description": "Return stored OAuth/API-key credential status without secrets.",
          "properties": {
            "kind": {
              "const": "auth.status",
              "type": "string"
            },
            "provider": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "provider"
          ],
          "type": "object"
        },
        {
          "description": "Ask the trusted host to mint OAuth lease metadata and, for trusted native\nclients, optionally include a short-lived access token. The stored refresh\ntoken is never returned.",
          "properties": {
            "force_refresh": {
              "default": false,
              "description": "Force the broker to refresh before returning a lease.",
              "type": "boolean"
            },
            "include_token": {
              "description": "Include the short-lived access token in the response. Defaults to true\nfor backwards-compatible native clients; browser clients should pass false.",
              "type": "boolean"
            },
            "kind": {
              "const": "auth.lease",
              "type": "string"
            },
            "provider": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "provider"
          ],
          "type": "object"
        },
        {
          "description": "Remove stored credentials for a provider.",
          "properties": {
            "kind": {
              "const": "auth.logout",
              "type": "string"
            },
            "provider": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "provider"
          ],
          "type": "object"
        },
        {
          "description": "Delegate a coding task to an external agent CLI (codex / claude / gemini)\nas a long-lived job. Pure protocol vocabulary: the host job manager drives\nthe subprocess (DA-2); `nerve-core` has no subprocess knowledge. `agent` is\nthe catalog name from `list_agents`; `cwd` defaults to the workspace root;\n`model` overrides the agent's default model. Progress streams back as\n[`crate::RuntimeEvent::DelegateProgress`].",
          "properties": {
            "agent": {
              "type": "string"
            },
            "autonomy": {
              "$ref": "#/$defs/DelegateAutonomy",
              "default": "read_only"
            },
            "cwd": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "delegate.start",
              "type": "string"
            },
            "mcp_enable": {
              "description": "DA-6 (codex only): the MCP allowlist for this delegated codex session —\nthe `[mcp_servers.<name>]` entries to keep enabled; every other\nconfigured server is disabled for a fast start. `Some(list)` overrides\nthe persisted `[delegate.codex] mcp_enable` config (an empty list\ndisables ALL); `None` falls back to that config. Ignored for non-codex\nagents.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "model": {
              "type": [
                "string",
                "null"
              ]
            },
            "role": {
              "$ref": "#/$defs/DelegateRole",
              "description": "Behavior preset for the delegated agent (DA-7). Defaults to\n[`DelegateRole::Standard`] (passthrough); [`DelegateRole::Scout`] makes\nit a read-only repository explorer that returns compact citations and\nforces read-only autonomy regardless of the `autonomy` field."
            },
            "task": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "agent",
            "task"
          ],
          "type": "object"
        },
        {
          "description": "Steer a live delegated session with a follow-up user message, running one\nmore turn against the same long-lived agent process. Pure protocol\nvocabulary: the host job manager looks up the live session (DA-5a) and\ncontinues it; progress streams back as [`crate::RuntimeEvent::DelegateProgress`].\n`session_id` is the `job_id` of the originating [`Self::DelegateStart`] job\n(a started delegated session keeps that id for its whole lifetime).",
          "properties": {
            "kind": {
              "const": "delegate.steer",
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id",
            "message"
          ],
          "type": "object"
        },
        {
          "description": "End a live delegated session: close the agent process's stdin (which it\ntreats as EOF and exits on) and reap it. Pure protocol vocabulary; the host\njob manager deregisters the live session. `session_id` is the originating\n[`Self::DelegateStart`] job id (see [`Self::DelegateSteer`]).",
          "properties": {
            "kind": {
              "const": "delegate.close",
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "Fetch one live delegated session by id (the originating\n[`Self::DelegateStart`] job id). Read-only; mirrors `session.get` /\n`flow.get`. An unknown id is an error. The result is\n`{ \"delegate\": { session_id, agent, status, agent_session_id } }`.",
          "properties": {
            "kind": {
              "const": "delegate.get",
              "type": "string"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "List the live delegated sessions the host is parking, so a cockpit can\nobserve its whole external-agent fleet over the protocol (not just from a\nsingle client's local state). Read-only; mirrors `session.list` /\n`flow.list`. The result is `{ \"delegates\": [ { session_id, agent, status,\nagent_session_id }, … ] }`, sorted by `session_id`.",
          "properties": {
            "kind": {
              "const": "delegate.list",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "List captured Runs — the L0 flight-recorder index (`trust-substrate.md`\n§6). Read-only; mirrors `delegate.list` / `flow.list`. Pure protocol\nvocabulary: the host reads the persisted `RunStore`; `nerve-core` has no\nstore. The result is `{ \"runs\": [ <Run>, … ] }`, newest `run_id` first.",
          "properties": {
            "kind": {
              "const": "run.list",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Fetch one captured Run by its content-addressed id (`trust-substrate.md`\n§6). Read-only; an unknown id is an error. The result is the full\n[`crate::provenance::Run`] (its event tape + content-addressed ledger), so\na client can replay or re-verify it.",
          "properties": {
            "kind": {
              "const": "run.get",
              "type": "string"
            },
            "run_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "run_id"
          ],
          "type": "object"
        },
        {
          "anyOf": [
            {
              "description": "An inline workflow definition (the whole strategy as data). Boxed so the\nlarge inline-def variant doesn't bloat every command value (the named-ref\nvariant is tiny).",
              "properties": {
                "workflow": {
                  "$ref": "#/$defs/WorkflowDef"
                }
              },
              "required": [
                "workflow"
              ],
              "type": "object"
            },
            {
              "description": "A named workflow resolved from a loaded data file (loaded, not compiled).",
              "properties": {
                "workflow_ref": {
                  "type": "string"
                }
              },
              "required": [
                "workflow_ref"
              ],
              "type": "object"
            }
          ],
          "description": "Start a declarative orchestration workflow (the Conductor, design §4) as one\ncancellable **job**: the host job manager runs the deterministic flow engine\n(C1) and the `job_id` IS the `flow_id`. The `workflow` is either an inline\n[`WorkflowDef`] or a named reference resolved from a loaded `WorkflowDef`\ndata file ([`FlowSource`]). Progress streams back as the `flow_*` events\n([`crate::RuntimeEvent::FlowStarted`] …); approvals reuse\n[`crate::RuntimeEvent::ApprovalRequested`] keyed by `flow_id`.",
          "properties": {
            "inputs": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Named-output seeds the engine exposes to the first node's task template.",
              "type": [
                "object",
                "null"
              ]
            },
            "kind": {
              "const": "flow.start",
              "type": "string"
            },
            "workspace": {
              "description": "Workspace to run against when more than one is registered.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Steer a live flow branch with a follow-up message, running one more turn\nagainst the same live worker session (design §4 / Wave C3a). Reuses the C0\n`WorkerSession::steer` port per-worker, exactly as `delegate.steer` does\nfor a single delegated session: the host job manager looks up the live\nworker for `target` in the live-flow worker registry and continues it;\nprogress streams back as [`crate::RuntimeEvent::FlowNodeAgent`]. Only a\nlive, steerable branch (a `Single`/`Pipeline` worker still in flight, on a\nsteerable substrate) can be steered; a closed or one-shot worker\n(`gemini`) returns a clear error. `flow_id` is the originating\n[`Self::FlowStart`] job id; `target` selects which branch (by node id, or\nthe only live worker when unset).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "const": "flow.steer",
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "target": {
              "$ref": "#/$defs/WorkerSelector"
            }
          },
          "required": [
            "kind",
            "flow_id",
            "message"
          ],
          "type": "object"
        },
        {
          "anyOf": [
            {
              "description": "Resolve the recorded ledger from the `FlowStore` by its `flow_id`.",
              "properties": {
                "flow_id": {
                  "type": "string"
                }
              },
              "required": [
                "flow_id"
              ],
              "type": "object"
            },
            {
              "description": "Replay a ledger at an explicit filesystem path (e.g. a copied tape).",
              "properties": {
                "ledger_path": {
                  "type": "string"
                }
              },
              "required": [
                "ledger_path"
              ],
              "type": "object"
            }
          ],
          "description": "Deterministically REPLAY a recorded flow offline (the audit verb, design §3/§4).\nThe host loads the recorded `WorkerLedger` from the `FlowStore` by\n[`LedgerRef`], runs the SAME deterministic engine in REPLAY mode — a\n`ReplayWorker` re-emits the recorded `WorkerEvent`s/`TurnResult`s instead of\ncalling any LLM/subprocess — and re-emits the `flow_*` event stream. Runs as\none cancellable **job** (the `job_id` IS the replayed `flow_id`); the replay is\nbyte-identical to the recorded run (the CI gate), at zero cost. `workspace`\nscopes which project's `.nerve/flows` the ledger is loaded from when more than\none is registered.",
          "properties": {
            "kind": {
              "const": "flow.replay",
              "type": "string"
            },
            "workspace": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Fetch one live or recently-finished flow by id. Mirrors `session.get`.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "const": "flow.get",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "flow_id"
          ],
          "type": "object"
        },
        {
          "description": "List flows the host knows about. Mirrors `session.list`.",
          "properties": {
            "kind": {
              "const": "flow.list",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Close (cancel) a live flow, tearing its workers down. Mirrors\n`session.close` / `delegate.close`. `flow_id` is the originating\n[`Self::FlowStart`] job id.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "const": "flow.close",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "flow_id"
          ],
          "type": "object"
        },
        {
          "description": "Reply to a flow approval request. **Reuses** the existing\n[`SessionApprovalDecision`] + the host `ApprovalHub` round-trip, keyed by\n`flow_id` (a flow branch is just another approval id). No new approval type.",
          "properties": {
            "decision": {
              "$ref": "#/$defs/SessionApprovalDecision"
            },
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "const": "flow.respond",
              "type": "string"
            },
            "request_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "flow_id",
            "request_id",
            "decision"
          ],
          "type": "object"
        },
        {
          "description": "Return concrete host/native affordances reachable through the daemon.",
          "properties": {
            "kind": {
              "const": "host.capabilities",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Write plain text to the host OS clipboard through the daemon.",
          "properties": {
            "kind": {
              "const": "host.clipboard.write_text",
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "Show a native OS notification through the host daemon.",
          "properties": {
            "body": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "host.notification.show",
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "title"
          ],
          "type": "object"
        },
        {
          "description": "Open a native host folder picker and return the selected absolute path.",
          "properties": {
            "kind": {
              "const": "host.folder.pick",
              "type": "string"
            },
            "title": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Save UTF-8 text through a native host save panel.",
          "properties": {
            "default_name": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "const": "host.file.save_text",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "title": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "Open an external http(s) URL with the host OS default handler.",
          "properties": {
            "kind": {
              "const": "host.url.open",
              "type": "string"
            },
            "url": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "url"
          ],
          "type": "object"
        },
        {
          "description": "Reveal a served workspace root in the OS file manager through the daemon.",
          "properties": {
            "kind": {
              "const": "workspace.reveal",
              "type": "string"
            },
            "workspace": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Begin a personal-WeChat (个人微信) QR login through the iLink Bot gateway as\na long-lived **job**: the host fetches a QR, streams `wechat` login events\n([`crate::WechatEventKind::LoginQr`] → `LoginStatus` → `LoggedIn`), and on\nsuccess caches the session so [`Self::WechatStart`] can run the bridge. Pure\nprotocol vocabulary; the daemon's WeChat host executes it (it is network +\nwall-clock, never `nerve-core`). The job is cancellable (cancel aborts the\nQR poll). `bot_type` is the iLink bot registration type; it defaults to\n`\"3\"` (`DEFAULT_ILINK_BOT_TYPE` — the value tools like Hermes Agent bake in),\nso login is **scan-only** and the field can be omitted.",
          "properties": {
            "base_url": {
              "description": "Login bootstrap host; defaults to the iLink default when omitted.",
              "type": [
                "string",
                "null"
              ]
            },
            "bot_type": {
              "default": "3",
              "type": "string"
            },
            "kind": {
              "const": "wechat.login",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Start the WeChat→nerve bridge against the logged-in session: each allowed\nowner's inbound message drives one `delegate.*` turn (read-only by default)\nand the reply is sent back to the chat. Requires a prior successful\n[`Self::WechatLogin`] and a daemon started with `--allow-delegate`. Account\nsafety is fail-closed: an empty `owners` list denies everyone. Progress and\nrelayed messages stream back as [`crate::RuntimeEvent::Wechat`] events.",
          "properties": {
            "agent": {
              "default": "claude",
              "description": "Delegate agent catalog name (`claude` / `codex` / `gemini`).",
              "type": "string"
            },
            "autonomy": {
              "$ref": "#/$defs/DelegateAutonomy",
              "default": "read_only",
              "description": "Autonomy granted to each delegated turn (defaults to read-only)."
            },
            "kind": {
              "const": "wechat.start",
              "type": "string"
            },
            "owners": {
              "default": [],
              "description": "WeChat user ids permitted to drive the agent (empty = deny all).",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Stop the running WeChat bridge (idempotent: a no-op when none is running).\nThe logged-in session is retained so a later [`Self::WechatStart`] needs no\nre-scan.",
          "properties": {
            "kind": {
              "const": "wechat.stop",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Report WeChat login + bridge status (logged-in account/user, whether the\nbridge is running, the configured owners). Returns immediately.",
          "properties": {
            "kind": {
              "const": "wechat.status",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "RuntimeEvent": {
      "description": "Runtime event emitted by human-facing adapters while executing jobs.",
      "oneOf": [
        {
          "properties": {
            "command": {
              "type": "string"
            },
            "job_id": {
              "type": "string"
            },
            "tool_name": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "job_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "command"
          ],
          "type": "object"
        },
        {
          "properties": {
            "current": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "job_id": {
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "stage": {
              "type": "string"
            },
            "total": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "type": {
              "const": "job_progress",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "stage",
            "message"
          ],
          "type": "object"
        },
        {
          "properties": {
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_cancel_requested",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_completed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "error": {
              "$ref": "#/$defs/RuntimeJobError"
            },
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_failed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "error"
          ],
          "type": "object"
        },
        {
          "properties": {
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_cancelled",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id"
          ],
          "type": "object"
        },
        {
          "description": "A structured step from the built-in agent loop, scoped to its job.",
          "properties": {
            "event": {
              "$ref": "#/$defs/AgentEventKind"
            },
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "agent",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "event"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session has been created or resumed.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session has started processing a user turn.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "turn_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session is ready for the next client action.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_idle",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session has been closed.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_closed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A structured agent-loop step scoped to an interactive session.",
          "properties": {
            "event": {
              "$ref": "#/$defs/AgentEventKind"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_agent",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "event"
          ],
          "type": "object"
        },
        {
          "description": "Advisory streaming fragment of an in-progress tool call, scoped to its\njob. Carries a raw provider delta string; UI-only and additive — clients\nthat don't render streaming tool calls may ignore it. The producer is\nwired in a later wave; this variant only reserves the protocol shape.",
          "properties": {
            "delta": {
              "type": "string"
            },
            "index": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "tool_call_delta",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "delta"
          ],
          "type": "object"
        },
        {
          "description": "A session turn needs a client/human decision before continuing.",
          "properties": {
            "arguments": true,
            "preview": {
              "default": "",
              "description": "Human-readable preview of what the call would do (e.g. a diff or\ncommand line). Additive; defaults to empty when not computed.",
              "type": "string"
            },
            "request_id": {
              "type": "string"
            },
            "session_id": {
              "type": "string"
            },
            "tier": {
              "$ref": "#/$defs/RiskTier",
              "default": "exec",
              "description": "Risk classification of the tool whose call is awaiting a decision.\nAdditive; older emitters/clients that omit it default to the\nmost-restricted tier ([`RiskTier::Exec`])."
            },
            "tool": {
              "type": "string"
            },
            "type": {
              "const": "approval_requested",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "request_id",
            "tool",
            "arguments"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed authentication lifecycle update.",
          "properties": {
            "kind": {
              "$ref": "#/$defs/AuthEventKind"
            },
            "provider": {
              "type": "string"
            },
            "type": {
              "const": "auth",
              "type": "string"
            }
          },
          "required": [
            "type",
            "provider",
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Streaming output fragment from a delegated external agent CLI, scoped to\nits job. `agent` is the catalog name (codex / claude / gemini); `text` is a\nraw stdout/stderr chunk. Additive and job-scoped; the producer is wired in\nDA-2 (this variant only reserves the protocol shape).",
          "properties": {
            "agent": {
              "type": "string"
            },
            "job_id": {
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "const": "delegate_progress",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "agent",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "A delegated run's tape has been sealed and persisted to the `RunStore` —\nthe L0 flight-recorder \"run recorded\" announcement (`trust-substrate.md`\n§6). `root_hash` is the content address committing to the whole ordered\ntape (`\"\"` if empty); a client fetches the full [`crate::provenance::Run`]\nvia `run.get`. **Global/unscoped** ([`Self::session_id`] returns `None`, like\n[`Self::Auth`] / [`Self::Wechat`]): a sealed run is a fleet-wide ledger event,\nso it is broadcast to every connected client and a fleet flight-recorder\ndashboard catches EVERY recorded run — not only the session it watches. The\n`session_id` field still names the originating `delegate.start` session for\nattribution. Additive; the payload is intentionally tiny (ids + root hash +\ncount), never the whole tape.",
          "properties": {
            "event_count": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "root_hash": {
              "type": "string"
            },
            "run_id": {
              "type": "string"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "run_recorded",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "run_id",
            "root_hash",
            "event_count"
          ],
          "type": "object"
        },
        {
          "description": "A flow run (the Conductor, design §4) has started. Carries the declarative\n[`Strategy`] so a client can render the DAG shape before any node runs. All\n`flow_*` events carry the `flow_id` (which is the flow job's id).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "strategy": {
              "$ref": "#/$defs/Strategy"
            },
            "type": {
              "const": "flow_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "strategy"
          ],
          "type": "object"
        },
        {
          "description": "A flow node's worker has started. `worker` is a human-readable label (the\nCLI catalog name or `provider/model`); `kind` is the worker family\n(`cli` | `provider`) so a client can badge the node pane.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/FlowWorkerKind"
            },
            "node_id": {
              "type": "string"
            },
            "type": {
              "const": "flow_node_started",
              "type": "string"
            },
            "worker": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "worker",
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "A flow node's worker has finished. `ok` is the node's success; `usage` is\nthe node's token usage (zeroed when the worker reported none).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "node_id": {
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "type": {
              "const": "flow_node_finished",
              "type": "string"
            },
            "usage": {
              "$ref": "#/$defs/FlowNodeUsage"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "ok",
            "usage"
          ],
          "type": "object"
        },
        {
          "description": "A DAG edge `from → to` between two flow nodes, for rendering the graph.\nEmitted as the engine wires a downstream node to its upstream producer.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "from": {
              "type": "string"
            },
            "to": {
              "type": "string"
            },
            "type": {
              "const": "flow_edge",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "from",
            "to"
          ],
          "type": "object"
        },
        {
          "description": "A structured agent-loop step scoped to a flow node — **reuses\n[`AgentEventKind`] verbatim**, symmetric with [`Self::SessionAgent`], so the\nTUI renders a node pane exactly as a session pane keyed by `node_id`.",
          "properties": {
            "event": {
              "$ref": "#/$defs/AgentEventKind"
            },
            "flow_id": {
              "type": "string"
            },
            "node_id": {
              "type": "string"
            },
            "type": {
              "const": "flow_node_agent",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "event"
          ],
          "type": "object"
        },
        {
          "description": "A flow finished with an aggregated outcome (the fold of the recorded node\nresults, in declared order — design §3).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "outcome": {
              "$ref": "#/$defs/FlowRunOutcome"
            },
            "type": {
              "const": "flow_completed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "outcome"
          ],
          "type": "object"
        },
        {
          "description": "A flow failed. `node_id` names the offending node when the failure is\nnode-local; `error` is a human-readable message.",
          "properties": {
            "error": {
              "type": "string"
            },
            "flow_id": {
              "type": "string"
            },
            "node_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "flow_failed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "error"
          ],
          "type": "object"
        },
        {
          "description": "Running fleet-budget telemetry (design §6): the cumulative cost + tokens\nspent across the whole flow tree, emitted after each node's `Usage` is\ndebited from the deterministic `BudgetLedger`. Additive within v4.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "spent_usd": {
              "format": "double",
              "type": "number"
            },
            "tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "type": {
              "const": "budget_update",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "spent_usd",
            "tokens"
          ],
          "type": "object"
        },
        {
          "description": "A fleet-budget warning (design §6): spend crossed a threshold (e.g. 80%)\nof a configured limit but has not yet been exhausted. `limit_usd` is the\nUSD ceiling the warning is relative to. Additive within v4.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "limit_usd": {
              "format": "double",
              "type": "number"
            },
            "spent_usd": {
              "format": "double",
              "type": "number"
            },
            "type": {
              "const": "budget_warning",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "spent_usd",
            "limit_usd"
          ],
          "type": "object"
        },
        {
          "description": "A typed, replayable audit-trail decision the engine made (design §4/§6):\na budget exhaustion, a depth/worker spawn refusal (absence-at-floor), and\n— in later waves — a vote tally / judge pick / debate round. `node_id`\nnames the node the decision is about (the synthetic root `\"flow\"` for a\nflow-wide decision); `kind` is the [`FlowDecisionKind`]. Additive within v4.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/FlowDecisionKind"
            },
            "node_id": {
              "type": "string"
            },
            "type": {
              "const": "flow_decision",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "A personal-WeChat (个人微信) bridge lifecycle update: a login QR/status\ntransition, the bridge's running state, or a relayed message. **Global /\nunscoped** (like [`Self::Auth`]): [`Self::session_id`] returns `None`, so the\nper-id fan-out delivers it to *every* connected client — any GUI/TUI surface\ncan render WeChat login + bridge status live without owning a session. The\ndaemon's WeChat host emits these; the payload is the typed\n[`WechatEventKind`].",
          "properties": {
            "kind": {
              "$ref": "#/$defs/WechatEventKind"
            },
            "type": {
              "const": "wechat",
              "type": "string"
            }
          },
          "required": [
            "type",
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "RuntimeEventCapabilities": {
      "properties": {
        "method": {
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    },
    "RuntimeEventNotification": {
      "description": "Params payload of a `runtime/event` notification: the event itself plus a\nmonotonic, per-stream sequence number so clients can detect dropped events\nand request replay. The event fields are flattened, so this is backward\ncompatible with clients that read the bare event from `params`; `event_seq`\nis an additive sibling field (defaulting to 0 until a later wave assigns the\nreal monotonic value at emit time).",
      "oneOf": [
        {
          "properties": {
            "command": {
              "type": "string"
            },
            "job_id": {
              "type": "string"
            },
            "tool_name": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "job_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "command"
          ],
          "type": "object"
        },
        {
          "properties": {
            "current": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "job_id": {
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "stage": {
              "type": "string"
            },
            "total": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "type": {
              "const": "job_progress",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "stage",
            "message"
          ],
          "type": "object"
        },
        {
          "properties": {
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_cancel_requested",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_completed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "error": {
              "$ref": "#/$defs/RuntimeJobError"
            },
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_failed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "error"
          ],
          "type": "object"
        },
        {
          "properties": {
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "job_cancelled",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id"
          ],
          "type": "object"
        },
        {
          "description": "A structured step from the built-in agent loop, scoped to its job.",
          "properties": {
            "event": {
              "$ref": "#/$defs/AgentEventKind"
            },
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "agent",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "event"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session has been created or resumed.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session has started processing a user turn.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "turn_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session is ready for the next client action.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_idle",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed session has been closed.",
          "properties": {
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_closed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A structured agent-loop step scoped to an interactive session.",
          "properties": {
            "event": {
              "$ref": "#/$defs/AgentEventKind"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session_agent",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "event"
          ],
          "type": "object"
        },
        {
          "description": "Advisory streaming fragment of an in-progress tool call, scoped to its\njob. Carries a raw provider delta string; UI-only and additive — clients\nthat don't render streaming tool calls may ignore it. The producer is\nwired in a later wave; this variant only reserves the protocol shape.",
          "properties": {
            "delta": {
              "type": "string"
            },
            "index": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "job_id": {
              "type": "string"
            },
            "type": {
              "const": "tool_call_delta",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "delta"
          ],
          "type": "object"
        },
        {
          "description": "A session turn needs a client/human decision before continuing.",
          "properties": {
            "arguments": true,
            "preview": {
              "default": "",
              "description": "Human-readable preview of what the call would do (e.g. a diff or\ncommand line). Additive; defaults to empty when not computed.",
              "type": "string"
            },
            "request_id": {
              "type": "string"
            },
            "session_id": {
              "type": "string"
            },
            "tier": {
              "$ref": "#/$defs/RiskTier",
              "default": "exec",
              "description": "Risk classification of the tool whose call is awaiting a decision.\nAdditive; older emitters/clients that omit it default to the\nmost-restricted tier ([`RiskTier::Exec`])."
            },
            "tool": {
              "type": "string"
            },
            "type": {
              "const": "approval_requested",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "request_id",
            "tool",
            "arguments"
          ],
          "type": "object"
        },
        {
          "description": "A host-managed authentication lifecycle update.",
          "properties": {
            "kind": {
              "$ref": "#/$defs/AuthEventKind"
            },
            "provider": {
              "type": "string"
            },
            "type": {
              "const": "auth",
              "type": "string"
            }
          },
          "required": [
            "type",
            "provider",
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "Streaming output fragment from a delegated external agent CLI, scoped to\nits job. `agent` is the catalog name (codex / claude / gemini); `text` is a\nraw stdout/stderr chunk. Additive and job-scoped; the producer is wired in\nDA-2 (this variant only reserves the protocol shape).",
          "properties": {
            "agent": {
              "type": "string"
            },
            "job_id": {
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "const": "delegate_progress",
              "type": "string"
            }
          },
          "required": [
            "type",
            "job_id",
            "agent",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "A delegated run's tape has been sealed and persisted to the `RunStore` —\nthe L0 flight-recorder \"run recorded\" announcement (`trust-substrate.md`\n§6). `root_hash` is the content address committing to the whole ordered\ntape (`\"\"` if empty); a client fetches the full [`crate::provenance::Run`]\nvia `run.get`. **Global/unscoped** ([`Self::session_id`] returns `None`, like\n[`Self::Auth`] / [`Self::Wechat`]): a sealed run is a fleet-wide ledger event,\nso it is broadcast to every connected client and a fleet flight-recorder\ndashboard catches EVERY recorded run — not only the session it watches. The\n`session_id` field still names the originating `delegate.start` session for\nattribution. Additive; the payload is intentionally tiny (ids + root hash +\ncount), never the whole tape.",
          "properties": {
            "event_count": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "root_hash": {
              "type": "string"
            },
            "run_id": {
              "type": "string"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "run_recorded",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "run_id",
            "root_hash",
            "event_count"
          ],
          "type": "object"
        },
        {
          "description": "A flow run (the Conductor, design §4) has started. Carries the declarative\n[`Strategy`] so a client can render the DAG shape before any node runs. All\n`flow_*` events carry the `flow_id` (which is the flow job's id).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "strategy": {
              "$ref": "#/$defs/Strategy"
            },
            "type": {
              "const": "flow_started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "strategy"
          ],
          "type": "object"
        },
        {
          "description": "A flow node's worker has started. `worker` is a human-readable label (the\nCLI catalog name or `provider/model`); `kind` is the worker family\n(`cli` | `provider`) so a client can badge the node pane.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/FlowWorkerKind"
            },
            "node_id": {
              "type": "string"
            },
            "type": {
              "const": "flow_node_started",
              "type": "string"
            },
            "worker": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "worker",
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "A flow node's worker has finished. `ok` is the node's success; `usage` is\nthe node's token usage (zeroed when the worker reported none).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "node_id": {
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "type": {
              "const": "flow_node_finished",
              "type": "string"
            },
            "usage": {
              "$ref": "#/$defs/FlowNodeUsage"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "ok",
            "usage"
          ],
          "type": "object"
        },
        {
          "description": "A DAG edge `from → to` between two flow nodes, for rendering the graph.\nEmitted as the engine wires a downstream node to its upstream producer.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "from": {
              "type": "string"
            },
            "to": {
              "type": "string"
            },
            "type": {
              "const": "flow_edge",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "from",
            "to"
          ],
          "type": "object"
        },
        {
          "description": "A structured agent-loop step scoped to a flow node — **reuses\n[`AgentEventKind`] verbatim**, symmetric with [`Self::SessionAgent`], so the\nTUI renders a node pane exactly as a session pane keyed by `node_id`.",
          "properties": {
            "event": {
              "$ref": "#/$defs/AgentEventKind"
            },
            "flow_id": {
              "type": "string"
            },
            "node_id": {
              "type": "string"
            },
            "type": {
              "const": "flow_node_agent",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "event"
          ],
          "type": "object"
        },
        {
          "description": "A flow finished with an aggregated outcome (the fold of the recorded node\nresults, in declared order — design §3).",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "outcome": {
              "$ref": "#/$defs/FlowRunOutcome"
            },
            "type": {
              "const": "flow_completed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "outcome"
          ],
          "type": "object"
        },
        {
          "description": "A flow failed. `node_id` names the offending node when the failure is\nnode-local; `error` is a human-readable message.",
          "properties": {
            "error": {
              "type": "string"
            },
            "flow_id": {
              "type": "string"
            },
            "node_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "flow_failed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "error"
          ],
          "type": "object"
        },
        {
          "description": "Running fleet-budget telemetry (design §6): the cumulative cost + tokens\nspent across the whole flow tree, emitted after each node's `Usage` is\ndebited from the deterministic `BudgetLedger`. Additive within v4.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "spent_usd": {
              "format": "double",
              "type": "number"
            },
            "tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "type": {
              "const": "budget_update",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "spent_usd",
            "tokens"
          ],
          "type": "object"
        },
        {
          "description": "A fleet-budget warning (design §6): spend crossed a threshold (e.g. 80%)\nof a configured limit but has not yet been exhausted. `limit_usd` is the\nUSD ceiling the warning is relative to. Additive within v4.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "limit_usd": {
              "format": "double",
              "type": "number"
            },
            "spent_usd": {
              "format": "double",
              "type": "number"
            },
            "type": {
              "const": "budget_warning",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "spent_usd",
            "limit_usd"
          ],
          "type": "object"
        },
        {
          "description": "A typed, replayable audit-trail decision the engine made (design §4/§6):\na budget exhaustion, a depth/worker spawn refusal (absence-at-floor), and\n— in later waves — a vote tally / judge pick / debate round. `node_id`\nnames the node the decision is about (the synthetic root `\"flow\"` for a\nflow-wide decision); `kind` is the [`FlowDecisionKind`]. Additive within v4.",
          "properties": {
            "flow_id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/FlowDecisionKind"
            },
            "node_id": {
              "type": "string"
            },
            "type": {
              "const": "flow_decision",
              "type": "string"
            }
          },
          "required": [
            "type",
            "flow_id",
            "node_id",
            "kind"
          ],
          "type": "object"
        },
        {
          "description": "A personal-WeChat (个人微信) bridge lifecycle update: a login QR/status\ntransition, the bridge's running state, or a relayed message. **Global /\nunscoped** (like [`Self::Auth`]): [`Self::session_id`] returns `None`, so the\nper-id fan-out delivers it to *every* connected client — any GUI/TUI surface\ncan render WeChat login + bridge status live without owning a session. The\ndaemon's WeChat host emits these; the payload is the typed\n[`WechatEventKind`].",
          "properties": {
            "kind": {
              "$ref": "#/$defs/WechatEventKind"
            },
            "type": {
              "const": "wechat",
              "type": "string"
            }
          },
          "required": [
            "type",
            "kind"
          ],
          "type": "object"
        }
      ],
      "properties": {
        "eventSeq": {
          "default": 0,
          "description": "Monotonically increasing, gap-detectable sequence number for this stream.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RuntimeInfo": {
      "properties": {
        "capabilities": {
          "$ref": "#/$defs/RuntimeCapabilities"
        },
        "protocol": {
          "type": "string"
        },
        "protocolVersion": {
          "type": "string"
        },
        "serverInfo": {
          "$ref": "#/$defs/RuntimeServerInfo"
        }
      },
      "required": [
        "protocol",
        "protocolVersion",
        "serverInfo",
        "capabilities"
      ],
      "type": "object"
    },
    "RuntimeJobCancelRequest": {
      "description": "Request payload for `runtime/jobs/cancel`.",
      "properties": {
        "job_id": {
          "type": "string"
        }
      },
      "required": [
        "job_id"
      ],
      "type": "object"
    },
    "RuntimeJobCancelResponse": {
      "properties": {
        "cancellation_requested": {
          "type": "boolean"
        },
        "job": {
          "$ref": "#/$defs/RuntimeJobSnapshot"
        }
      },
      "required": [
        "cancellation_requested",
        "job"
      ],
      "type": "object"
    },
    "RuntimeJobCapabilities": {
      "properties": {
        "commandKinds": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "methods": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "methods",
        "commandKinds"
      ],
      "type": "object"
    },
    "RuntimeJobError": {
      "description": "Structured error stored on failed runtime jobs.",
      "properties": {
        "kind": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "message"
      ],
      "type": "object"
    },
    "RuntimeJobGetRequest": {
      "description": "Request payload for `runtime/jobs/get`.",
      "properties": {
        "include_result": {
          "default": true,
          "type": "boolean"
        },
        "job_id": {
          "type": "string"
        }
      },
      "required": [
        "job_id"
      ],
      "type": "object"
    },
    "RuntimeJobListRequest": {
      "description": "Request payload for `runtime/jobs/list`.",
      "properties": {
        "include_results": {
          "default": false,
          "type": "boolean"
        },
        "include_terminal": {
          "default": true,
          "type": "boolean"
        },
        "limit": {
          "default": 100,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RuntimeJobListResponse": {
      "properties": {
        "jobs": {
          "items": {
            "$ref": "#/$defs/RuntimeJobSnapshot"
          },
          "type": "array"
        }
      },
      "required": [
        "jobs"
      ],
      "type": "object"
    },
    "RuntimeJobResponse": {
      "properties": {
        "job": {
          "$ref": "#/$defs/RuntimeJobSnapshot"
        }
      },
      "required": [
        "job"
      ],
      "type": "object"
    },
    "RuntimeJobSnapshot": {
      "description": "Snapshot of a runtime job owned by a transport/daemon implementation.",
      "properties": {
        "cancel_requested": {
          "type": "boolean"
        },
        "command": {
          "type": "string"
        },
        "created_at_ms": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "error": {
          "anyOf": [
            {
              "$ref": "#/$defs/RuntimeJobError"
            },
            {
              "type": "null"
            }
          ]
        },
        "finished_at_ms": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "job_id": {
          "type": "string"
        },
        "result": true,
        "started_at_ms": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/RuntimeJobStatus"
        },
        "tool_name": {
          "type": [
            "string",
            "null"
          ]
        },
        "updated_at_ms": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "job_id",
        "status",
        "command",
        "created_at_ms",
        "updated_at_ms",
        "cancel_requested"
      ],
      "type": "object"
    },
    "RuntimeJobStartRequest": {
      "description": "Request payload for `runtime/jobs/start`.",
      "properties": {
        "command": {
          "$ref": "#/$defs/RuntimeCommand"
        },
        "job_id": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "RuntimeJobStatus": {
      "description": "Status values used by daemon-owned runtime jobs.",
      "enum": [
        "running",
        "cancelling",
        "completed",
        "failed",
        "cancelled"
      ],
      "type": "string"
    },
    "RuntimeServerInfo": {
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "RuntimeToolSpec": {
      "additionalProperties": true,
      "description": "Runtime-visible tool specification shape shared with frontend clients.",
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "inputSchema": true,
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "RuntimeToolsListResponse": {
      "properties": {
        "tools": {
          "items": {
            "$ref": "#/$defs/RuntimeToolSpec"
          },
          "type": "array"
        }
      },
      "required": [
        "tools"
      ],
      "type": "object"
    },
    "RuntimeTransportCapabilities": {
      "properties": {
        "framing": {
          "type": "string"
        },
        "jsonrpc": {
          "type": "string"
        }
      },
      "required": [
        "jsonrpc",
        "framing"
      ],
      "type": "object"
    },
    "SessionApprovalDecision": {
      "description": "Decision supplied by a human/client for a session approval request.\n\n`Allow`/`Deny` apply to this call only; `AllowAlways`/`DenyAlways` additionally\nsignal the host to remember the decision for future calls (P2 wires the\nremembering; P1 only distinguishes allow-vs-deny via [`Self::allows`]).",
      "oneOf": [
        {
          "const": "allow",
          "description": "Allow this call only.",
          "type": "string"
        },
        {
          "const": "deny",
          "description": "Deny this call only.",
          "type": "string"
        },
        {
          "const": "allow_always",
          "description": "Allow this call and remember the allow for future matching calls.",
          "type": "string"
        },
        {
          "const": "deny_always",
          "description": "Deny this call and remember the deny for future matching calls.",
          "type": "string"
        }
      ]
    },
    "Step": {
      "description": "One unit of orchestrated work: which worker, what task, how autonomous, and\nwhat to do if it fails.",
      "properties": {
        "autonomy": {
          "$ref": "#/$defs/DelegateAutonomy",
          "default": "read_only",
          "description": "Autonomy posture (maps to the CLI sandbox flag / provider exec capability)."
        },
        "on_fail": {
          "$ref": "#/$defs/FailPolicy",
          "default": {
            "kind": "abort"
          },
          "description": "What the engine does when this step's worker fails."
        },
        "task": {
          "$ref": "#/$defs/TaskTemplate",
          "description": "The task handed to the worker (with minimal named-output interpolation)."
        },
        "worker": {
          "$ref": "#/$defs/WorkerRef",
          "description": "Which worker runs this step."
        }
      },
      "required": [
        "worker",
        "task"
      ],
      "type": "object"
    },
    "Strategy": {
      "description": "The closed, additive-versioned set of orchestration combinators (design §3).\n\nC1 implements `Single` + `Parallel`; the rest are defined-ahead for C5 so the\ndata is stable. `#[non_exhaustive]` keeps adding variants additive for\ndownstream matchers, and the engine's interpreter matches every variant\nexplicitly (returning `Terminate` with an \"unimplemented strategy\" outcome\nfor the not-yet-wired ones), so the totality is compile-checked.",
      "oneOf": [
        {
          "description": "Run one worker and return its result (design §3). **Implemented in C1.**",
          "properties": {
            "step": {
              "$ref": "#/$defs/Step"
            },
            "type": {
              "const": "single",
              "type": "string"
            }
          },
          "required": [
            "type",
            "step"
          ],
          "type": "object"
        },
        {
          "description": "Fan out to N branches in parallel, then fold by `join` in **declared\norder** (design §3, the load-bearing invariant). **Implemented in C1.**",
          "properties": {
            "branches": {
              "items": {
                "$ref": "#/$defs/Step"
              },
              "type": "array"
            },
            "join": {
              "$ref": "#/$defs/Join"
            },
            "type": {
              "const": "parallel",
              "type": "string"
            }
          },
          "required": [
            "type",
            "branches",
            "join"
          ],
          "type": "object"
        },
        {
          "description": "Run stages in sequence; stage N reads the ledger outputs of stages `< N`.\nDefined-ahead for C3/C5.",
          "properties": {
            "stages": {
              "items": {
                "$ref": "#/$defs/Step"
              },
              "type": "array"
            },
            "type": {
              "const": "pipeline",
              "type": "string"
            }
          },
          "required": [
            "type",
            "stages"
          ],
          "type": "object"
        },
        {
          "description": "Map a step over a context split, then reduce. Defined-ahead for C5.",
          "properties": {
            "map": {
              "$ref": "#/$defs/Step"
            },
            "over": {
              "$ref": "#/$defs/ContextSplit"
            },
            "reduce": {
              "$ref": "#/$defs/Step"
            },
            "type": {
              "const": "map_reduce",
              "type": "string"
            }
          },
          "required": [
            "type",
            "map",
            "over",
            "reduce"
          ],
          "type": "object"
        },
        {
          "description": "Generate `candidates`, keep the top `k`, and let a `judge` pick.\nDefined-ahead for C5.",
          "properties": {
            "candidates": {
              "items": {
                "$ref": "#/$defs/Step"
              },
              "type": "array"
            },
            "judge": {
              "$ref": "#/$defs/Step"
            },
            "k": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "type": {
              "const": "vote_judge",
              "type": "string"
            }
          },
          "required": [
            "type",
            "candidates",
            "judge",
            "k"
          ],
          "type": "object"
        },
        {
          "description": "Multi-round debate between `sides`, adjudicated by `judge`. Defined-ahead\nfor C5.",
          "properties": {
            "judge": {
              "$ref": "#/$defs/Step"
            },
            "rounds": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "sides": {
              "items": {
                "$ref": "#/$defs/Step"
              },
              "type": "array"
            },
            "type": {
              "const": "debate",
              "type": "string"
            }
          },
          "required": [
            "type",
            "sides",
            "rounds",
            "judge"
          ],
          "type": "object"
        },
        {
          "description": "A planner emits a child strategy the engine runs bounded by `max_depth`\n(design §8). Defined-ahead for C5.",
          "properties": {
            "child": {
              "$ref": "#/$defs/Strategy"
            },
            "planner": {
              "$ref": "#/$defs/Step"
            },
            "type": {
              "const": "hierarchical",
              "type": "string"
            }
          },
          "required": [
            "type",
            "planner",
            "child"
          ],
          "type": "object"
        }
      ]
    },
    "TaskTemplate": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "properties": {
            "prompt": {
              "type": "string"
            }
          },
          "required": [
            "prompt"
          ],
          "type": "object"
        }
      ],
      "description": "A minimal task template (design §3, open question 3): a plain prompt plus\noptional `{{name}}` placeholders substituted from named ledger outputs. **No\nexpression language** — named-output substitution only, deliberately."
    },
    "WechatEventKind": {
      "description": "The typed payload of a [`RuntimeEvent::Wechat`] — one WeChat-bridge lifecycle\nstep. A closed, additive-versioned enum (mirroring [`AuthEventKind`]) so a\nclient renders login progress, the running state, and the message log from one\nevent family. Defined as transport-neutral data; the host maps its gateway\nstates onto these.",
      "oneOf": [
        {
          "description": "A QR is ready to scan: `image_url` is a remote HTTPS image URL (safe to put\nin an `<img src>`), `qrcode` is its opaque id.",
          "properties": {
            "image_url": {
              "type": "string"
            },
            "kind": {
              "const": "login_qr",
              "type": "string"
            },
            "qrcode": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "qrcode",
            "image_url"
          ],
          "type": "object"
        },
        {
          "description": "A login status transition before confirmation — the gateway's `status`\nstring (e.g. `scaned`, `expired`, `need_verifycode`).",
          "properties": {
            "kind": {
              "const": "login_status",
              "type": "string"
            },
            "status": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "status"
          ],
          "type": "object"
        },
        {
          "description": "Login succeeded; the bridge can now be started for this account.",
          "properties": {
            "account_id": {
              "type": "string"
            },
            "kind": {
              "const": "logged_in",
              "type": "string"
            },
            "user_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "account_id",
            "user_id"
          ],
          "type": "object"
        },
        {
          "description": "Login failed, timed out, or the QR expired.",
          "properties": {
            "error": {
              "type": "string"
            },
            "kind": {
              "const": "login_failed",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "error"
          ],
          "type": "object"
        },
        {
          "description": "The bridge's running state changed (started / stopped) for an account.",
          "properties": {
            "account_id": {
              "type": "string"
            },
            "kind": {
              "const": "bridge_status",
              "type": "string"
            },
            "running": {
              "type": "boolean"
            },
            "user_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "running",
            "account_id",
            "user_id"
          ],
          "type": "object"
        },
        {
          "description": "A message relayed across the bridge — `direction` is `\"in\"` (owner → agent)\nor `\"out\"` (agent → owner) — for a live activity log. `chat_key` namespaces\nthe conversation; `from_user_id` is the WeChat sender.",
          "properties": {
            "chat_key": {
              "type": "string"
            },
            "direction": {
              "type": "string"
            },
            "from_user_id": {
              "type": "string"
            },
            "kind": {
              "const": "message",
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "chat_key",
            "from_user_id",
            "direction",
            "text"
          ],
          "type": "object"
        }
      ]
    },
    "WorkerRef": {
      "description": "Which worker runs a [`Step`]. The only place the CLI-vs-provider distinction\nis encoded in the data (design §7); the engine resolves it through the\n`WorkerFactory` to a kind-agnostic `AgentWorker`.",
      "oneOf": [
        {
          "description": "An external agentic CLI by catalog name (`codex` | `claude` | `gemini`).",
          "properties": {
            "kind": {
              "const": "cli",
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "An in-process provider loop by provider + model.",
          "properties": {
            "kind": {
              "const": "provider",
              "type": "string"
            },
            "model": {
              "type": "string"
            },
            "provider": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "provider",
            "model"
          ],
          "type": "object"
        },
        {
          "description": "A worker resolved from a named `WorkerDef` data file (design §6, C6).\nDefined ahead; the C1 engine does not resolve `Named` yet.",
          "properties": {
            "kind": {
              "const": "named",
              "type": "string"
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        }
      ]
    },
    "WorkerSelector": {
      "description": "Which live branch a [`RuntimeCommand::FlowSteer`] targets (design §4, the\n`WorkerSelector` row). Deliberately MINIMAL: a flow branch is identified by its\ndeterministic node id, or — when `node_id` is unset — \"the single live worker\"\n(the common case for a `Single`/`Pipeline` flow, which has exactly one live\nbranch at a time). An ambiguous unset selector against a multi-branch live flow\nis refused by the host with a clear message; the closed enum keeps the steer\nsurface from drifting into a query language.",
      "properties": {
        "node_id": {
          "description": "The deterministic node id of the branch to steer (e.g. `\"node-0\"` for a\n`Single` flow, `\"stage-1\"` for a pipeline's second stage). `None` targets\nthe only live worker, erroring if more than one is live.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "WorkflowDef": {
      "description": "A complete, declarative workflow: a named [`Strategy`] plus the fleet-wide\ngovernance envelope (design §3). `schema_version` is bumped only on a\nbreaking change to the data shape (additive fields don't bump it).",
      "properties": {
        "budget": {
          "$ref": "#/$defs/BudgetSpec",
          "default": {},
          "description": "Fleet budget envelope (design §6). Threaded as a placeholder in C1 — the\ndebit/cancel loop lands in C3."
        },
        "max_depth": {
          "default": 2,
          "description": "Hierarchy depth ceiling (design §8). Default 2, matching today's\n`DEFAULT_MAX_DEPTH`. Threaded as a placeholder in C1.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "name": {
          "description": "A human-readable name for the workflow (surfaced in logs / the ledger).",
          "type": "string"
        },
        "schema_version": {
          "description": "The on-disk schema version of this workflow document (current: `1`).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "strategy": {
          "$ref": "#/$defs/Strategy",
          "description": "The orchestration strategy the engine interprets."
        }
      },
      "required": [
        "schema_version",
        "name",
        "strategy"
      ],
      "type": "object"
    }
  },
  "properties": {
    "approvalMode": {
      "$ref": "#/$defs/ApprovalMode"
    },
    "hostCapabilities": {
      "$ref": "#/$defs/HostCapabilities"
    },
    "jsonValue": true,
    "run": {
      "$ref": "#/$defs/Run",
      "description": "L0 provenance shape root: a `run.get` returns a [`crate::provenance::Run`],\nwhich transitively pulls `Event` / `EventKind` / `LedgerEntry` into the\nexported schema (they are not reachable from any wire field on their own —\n`run_recorded` carries only ids + the root hash)."
    },
    "runtimeCommand": {
      "$ref": "#/$defs/RuntimeCommand"
    },
    "runtimeEvent": {
      "$ref": "#/$defs/RuntimeEvent"
    },
    "runtimeEventNotification": {
      "$ref": "#/$defs/RuntimeEventNotification"
    },
    "runtimeInfo": {
      "$ref": "#/$defs/RuntimeInfo"
    },
    "runtimeJob": {
      "$ref": "#/$defs/RuntimeJobSnapshot"
    },
    "runtimeJobCancelRequest": {
      "$ref": "#/$defs/RuntimeJobCancelRequest"
    },
    "runtimeJobCancelResponse": {
      "$ref": "#/$defs/RuntimeJobCancelResponse"
    },
    "runtimeJobError": {
      "$ref": "#/$defs/RuntimeJobError"
    },
    "runtimeJobGetRequest": {
      "$ref": "#/$defs/RuntimeJobGetRequest"
    },
    "runtimeJobListRequest": {
      "$ref": "#/$defs/RuntimeJobListRequest"
    },
    "runtimeJobListResponse": {
      "$ref": "#/$defs/RuntimeJobListResponse"
    },
    "runtimeJobResponse": {
      "$ref": "#/$defs/RuntimeJobResponse"
    },
    "runtimeJobStartRequest": {
      "$ref": "#/$defs/RuntimeJobStartRequest"
    },
    "runtimeToolSpec": {
      "$ref": "#/$defs/RuntimeToolSpec"
    },
    "runtimeToolsListResponse": {
      "$ref": "#/$defs/RuntimeToolsListResponse"
    }
  },
  "required": [
    "jsonValue",
    "runtimeCommand",
    "approvalMode",
    "runtimeEvent",
    "run",
    "runtimeEventNotification",
    "runtimeInfo",
    "hostCapabilities",
    "runtimeToolSpec",
    "runtimeJobError",
    "runtimeJob",
    "runtimeJobStartRequest",
    "runtimeJobGetRequest",
    "runtimeJobListRequest",
    "runtimeJobCancelRequest",
    "runtimeToolsListResponse",
    "runtimeJobResponse",
    "runtimeJobListResponse",
    "runtimeJobCancelResponse"
  ],
  "title": "RuntimeProtocolSchema",
  "type": "object"
}
